import { Button } from "@/ui/Button/Button";
import React from "react";

const page = () => {
  return (
    <div>
      <Button label="New template" primary={true} size="medium" />
    </div>
  );
};

export default page;
